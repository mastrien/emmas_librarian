// Single source of truth for contracts shared with the renderer (IpcChannel, domain types).
// This file used to be a hand-maintained copy of src/types that could drift out of sync.
export * from '../src/types';
